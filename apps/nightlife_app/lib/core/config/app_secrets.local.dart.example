/// Local platform API secrets for Vexda mobile development.
///
/// Copy this file to [app_secrets.local.dart] (gitignored) and set your local
/// Routes API key once. See docs/platform/API_KEYS_SETUP.md.
///
/// Do not commit real keys. CI generates `app_secrets.local.dart` from
/// `VEXDA_ROUTES_API_KEY`.
const String kLocalRoutesApiKey = '';
