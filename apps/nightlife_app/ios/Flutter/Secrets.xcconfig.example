// Untracked local iOS secrets — copy from Secrets.xcconfig.example (gitignored).
// CI generates this file from VEXDA_IOS_MAPS_API_KEY.
// Restrict the Maps SDK key to bundle identifier com.drinkspot.app.
MAPS_API_KEY=
