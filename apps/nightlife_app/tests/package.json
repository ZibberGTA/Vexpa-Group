{
  "name": "nightlife-firebase-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --config ../firebase.emulators.json \"jest --runInBand firestore.rules.test.js\"",
    "test:storage-rules": "firebase emulators:exec --config ../firebase.emulators.json \"jest --runInBand storage.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "jest": "^29.7.0"
  }
}
