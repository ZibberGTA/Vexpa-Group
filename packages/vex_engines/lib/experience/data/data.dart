/// Experience repository interfaces — no Firebase implementations in the engine.
library;

// Version 1 reads remain in VexCore. Write adapters stay in app shells until
// a later migration batch adds engine-owned write contracts here.
