Engine unit tests live in `packages/vex_engines/test/experience/`.

Run:

```bash
cd packages/vex_engines
dart test test/experience/
```

App parity tests remain in `apps/nightlife_web/test/` for drinks, deals, and events.
