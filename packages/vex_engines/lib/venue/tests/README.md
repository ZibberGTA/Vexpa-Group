# Tests

Engine unit tests mirror VexCore pilot testing style:

- domain rule tests (profile completion, validation)
- application orchestration with mocked repositories
- shared helper tests migrated from app test folders

No live Firebase. No widget tests (those stay in app `test/` folders).
