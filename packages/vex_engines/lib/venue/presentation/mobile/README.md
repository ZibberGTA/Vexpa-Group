# Mobile presentation

Mobile-specific view models and mappers for venue screens.

Screens stay in `apps/nightlife_app`. Shared orchestration should live in
`application/`; platform-specific formatting stays here.
