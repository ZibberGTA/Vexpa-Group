# Web presentation

Web-specific view models, section mappers, and formatting helpers for:

- public venue profile sections
- venue management dashboard view state

Flutter widgets remain in `apps/nightlife_web` until engine presentation
contracts stabilise. This folder holds mapping logic that can be unit tested
without widget tests.
