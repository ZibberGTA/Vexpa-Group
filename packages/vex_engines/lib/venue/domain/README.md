# Domain

Venue-specific entities and pure business rules:

- profile completion rules
- opening-hours invariants
- contact field validation
- gallery ownership rules
- dashboard tab and stat value objects (engine-neutral)

No Flutter. No Firebase. No repository interfaces (those live in `data/`).
