# Data

Engine-level repository interfaces and DTO mapping contracts for venue management:

- profile writes
- gallery metadata
- dashboard aggregates
- activity feeds

Implementations remain in web/mobile adapter folders during early phases.
Public reads continue to use VexCore repository contracts.
