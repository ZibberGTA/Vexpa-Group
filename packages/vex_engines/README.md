# Vex Engines

Vex Engines will contain Vexda business capability modules such as discovery, claims, trails, analytics, growth, and intelligence.

Foundation 1.0 only creates the package boundary and engine folders. Final engine APIs and Firebase adapters are deliberately excluded.

Engines may depend on VexCore contracts. Engines must not depend directly on Firebase SDKs, Flutter UI, or another engine's private persistence model.
